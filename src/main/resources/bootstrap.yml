server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Prempeh-Gyan/1.1-Config-Repo # This config server is integrated with GitHub to serve property files from a repository
          search-paths:
          - services/** # Root folder in GitHub repository from which the config server should look for property files
        encrypt:
          enabled: false
      enabled: false
encrypt:
  key: ALOMO2SHOTSFürSIE
management:
  security:
    enabled: false
    
    